.DS_Store
public/images/data
config/database.yml
#config/deploy*
config/localsettings.rb
intermix.tmproj
public/ckeditor_assets
*~
secrets

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore uploaded files in development.
/storage/*
!/storage/.keep

/public/assets
.byebug_history

# Ignore master key for decrypting credentials and more, and other secrets
/config/master.key
/config/secrets.yml
/config/initializers/secret_token.rb

/public/packs
/public/packs-test
/node_modules
/yarn-error.log
yarn-debug.log*
.yarn-integrity

/public/sitemap*.xml.gz

.claude/

# Local project/process meta files — kept out of git so they don't diverge
# between the master and production branches (they're per-working-copy notes,
# not deployable code).
CLAUDE.md
PROJECT.md
STATUS.md
REGISTRY.md
CONVENTIONS.md
LOG.md
ORCHESTRATION.md
AUDIT.md
