<h1>Sign In</h1>

<% if not ((@dialog and not @dialog.alt_logins) or (@group and not @group.alt_logins)) -%>

<% if @authentications %>
  <% unless @authentications.empty? %>
    <p><strong>You can sign in to this account using:</strong></p>
    <div class="authentications">
      <% for authentication in @authentications %>
        <div class="authentication">
          <%= image_tag "/images/login/#{authentication.provider}_32.png", :size => "32x32" %>
          <div class="provider"><%= authentication.provider.titleize %></div>
          <div class="uid"><%= authentication.uid %></div>
          <%= link_to "X", authentication, :confirm => 'Are you sure you want to remove this authentication option?', :method => :delete, :class => "remove" %>
        </div>
      <% end %>
      <div class="clear"></div>
    </div>
  <% end %>
  <p><strong>Add another service to sign in with:</strong></p>
<% else %>
  <p><strong>Sign in through one of these services:</strong></p>
<% end %>

<a href="/participants/auth/facebook" class="auth_provider">
  <%= image_tag "/images/login/facebook_64.png", :size => "64x64", :alt => "Facebook" %>
  Facebook
</a>
<a href="/participants/auth/twitter" class="auth_provider">
  <%= image_tag "/images/login/twitter_64.png", :size => "64x64", :alt => "Twitter" %>
  Twitter
</a>
<% if false -%>
<a href="/participants/auth/google_apps" class="auth_provider">
  <%= image_tag "/images/login/google_64.png", :size => "64x64", :alt => "Google" %>
  Google
</a>
<a href="/participants/auth/open_id" class="auth_provider">
  <%= image_tag "/images/login/openid_64.png", :size => "64x64", :alt => "OpenID" %>
  OpenID
</a>
<% end -%>
<div class="clear"></div>

<% end -%>

<p>If you prefer, you can instead <%= link_to('sign up', new_participant_registration_path) %> for an Intermix account with a password.</p>

<p>If you have an Intermix account, you can <%= link_to('sign in', new_participant_session_path)  %> with your e-mail address and password.</p>




