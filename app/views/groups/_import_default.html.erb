{{ group.logo }}

<p>You have been added to the group "{{ group.name }}" by {{ current_participant.name }}, an administrator of the group.</p>

<p>Your username: {{ participant.email }}<br>
Your password: {{ password }}</p>

<p>It would be a good idea to now go <a href="http://{{ domain }}/me/profile/edit?auth_token={{ participant.authentication_token }}">update your profile</a>, after which you can access the <a href="http://{{ domain }}/groups/{{ group.id }}/forum?auth_token={{ participant.authentication_token }}">group forum</a>.</p>
