<%= render(:partial => 'groups/menu')%>

<h3 style="clear:both">Import</h3>


<form action="/groups/<%= @group.id %>/importdo" method="post">
<%= hidden_field_tag :authenticity_token, form_authenticity_token %>

<% if false -%>	
<p>Enter names and addresses, in this format: Email,First,Last,Gender(M/F),Country of origin. Last two optional. One person per line:</p>
<% end -%>

<p>Enter names and addresses, in this format: Email,First,Last. One person per line:</p>

<p><%= text_area_tag 'new_text', nil, :rows => 5, :cols => 30 %></p>

<p><input type="submit" value="Submit"></p>

<p>Optional e-mail message to send to new members:<br><%= text_area_tag 'messtext', @messtext, :rows => 3, :cols => 30 %></p>

</form>